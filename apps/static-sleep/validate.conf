# TEMPORARY — validation only. Do NOT measure I_sleep with this file in the build.
# Re-enables a console on the native USB-Serial/JTAG so the printk in main.c is
# visible on /dev/ttyACM0. Remove (rebuild without -DEXTRA_CONF_FILE) before measuring.
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_SERIAL_ESP32_USB=y
